# enc-ctrl default configuration
# These values map to GopConfig fields.

max_gop_length=300
min_gop_length=30
defer_timeout_frames=60
scene_change_threshold=325
scene_change_holdoff=2
enable_variable_gop=0
idr_qp_boost=4
